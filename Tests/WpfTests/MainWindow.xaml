<Window x:Class="WpfTests.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfTests"
        xmlns:vm="clr-namespace:WpfTests.ViewModels"
        xmlns:commands="clr-namespace:WpfTests.Infrastructure.Commands"
        mc:Ignorable="d"
        Title="{Binding Title}"
        Topmost="{Binding IsChecked, ElementName=IsWindowTopMost}"
        Height="450" Width="500" ResizeMode="NoResize">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Открыть файл" Click="OnOpenFileClick" Name="openFile"/>
            <MenuItem Header="Отменить" Click="OnCancelReadingClick" Name="cancelReading" IsEnabled="False"/>
        </Menu>
        <ProgressBar DockPanel.Dock="Bottom" Height="20" Maximum="1" Minimum="0" Name="progressInfo"/>
        <Grid>
            <TextBlock Name="tbResult" Text="Результат"
                       VerticalAlignment="Center" HorizontalAlignment="Center"
                       FontWeight="Bold" FontSize="20"/>
        </Grid>
    </DockPanel>
</Window>
